import 'package:flutter/material.dart';

class SelectIconBottomSheet extends StatelessWidget {
  const SelectIconBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
